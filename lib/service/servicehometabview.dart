import 'package:flutter/material.dart';

class ServiceHomeTabView extends StatelessWidget {
  const ServiceHomeTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}